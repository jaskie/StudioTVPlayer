<UserControl x:Class="StudioTVPlayer.View.Main.Player.RundownItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StudioTVPlayer.View"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800"
             Height="auto"
             HorizontalAlignment="Stretch">
    <UserControl.Resources>
        <Style x:Key="MainBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding DragOver}" Value="True">
                    <Setter Property="BorderThickness" Value="0,5,0,0" />
                    <Setter Property="BorderBrush" Value="Black" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="MainContainerStyle" TargetType="{x:Type Grid}">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDisabled}" Value="True">
                    <Setter Property="Background" Value="#50404040" />
                </DataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsDisabled}" Value="False" />
                        <Condition Binding="{Binding IsVerified}" Value="False" />
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Background" Value="#50FF0000" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsDisabled}" Value="True" />
                        <Condition Binding="{Binding IsVerified}" Value="False" />
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="Background" Value="#20FF0000" />
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="IsDisabledStatusControl" TargetType="{x:Type Ellipse}">
            <Setter Property="Fill" Value="Transparent" />
            <Setter Property="Stroke" Value="{Binding Foreground, ElementName=textbox}" />
            <Setter Property="StrokeThickness" Value="1" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                    <Setter Property="Fill" Value="Green" />
                    <Setter Property="Stroke" Value="LightGreen" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Border Style="{StaticResource MainBorderStyle}">
        <Grid Style="{StaticResource MainContainerStyle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Ellipse Style="{StaticResource IsDisabledStatusControl}" Stretch="Uniform" VerticalAlignment="Center" Margin="5,0,0,0" Width="15" Height="{Binding Width, RelativeSource={RelativeSource Self}}" />
            <TextBlock Grid.Column="1" x:Name="textbox" Text="{Binding Name}" Margin="5,5,0,5" />
            <Rectangle Grid.Column="2" Fill="{Binding Foreground, ElementName=textbox}" VerticalAlignment="Stretch" Width="1" Margin="5" />
            <TextBlock Grid.Column="3" Text="{Binding Duration, StringFormat={}{0:hh}:{0:mm}:{0:ss}}" Margin="0,5,0,5" />
            <Rectangle Grid.Column="4" Fill="{Binding Foreground, ElementName=textbox}" VerticalAlignment="Stretch" Width="1" Margin="5" />
            <TextBlock Grid.Column="5" Text="{Binding CreationDate, StringFormat='yyyy-MM-dd HH:mm:ss '}" Margin="0,5,5,5" />
        </Grid>
    </Border>
</UserControl>
